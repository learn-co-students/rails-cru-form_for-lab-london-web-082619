<h1>All Songs</h1>

<% @songs.each do |song| %>
    <p><%=link_to song.name, song_path(song)%> - (<%= link_to song.genre.name, genre_path(song.genre) %>) - <%= link_to song.artist.name, artist_path(song.artist) %></p>
<% end %>