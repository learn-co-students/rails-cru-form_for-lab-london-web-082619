<%= form_for(@song) do |f| %>
    <%= f.label :name, "Song name:" %>
    <%= f.text_field :name %>

    <%= f.label :artist%>
    <%= f.collection_select(:artist_id, Artist.all, :id, :name) %>

    <%= f.submit %>
<% end %>